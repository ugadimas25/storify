## Packages
framer-motion | For smooth page transitions and micro-interactions
lucide-react | For beautiful iconography (already in base, but ensuring usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
- Audio playback needs a global context provider to persist across page navigation.
- Authentication is handled via Replit Auth (useAuth hook).
- Images are sourced from Unsplash for the MVP.
- Mobile-first design: Navigation bar at the bottom for easy thumb access.
